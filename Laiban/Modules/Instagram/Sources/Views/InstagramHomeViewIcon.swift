
import SwiftUI

public struct InstagramHomeViewIcon : View {
    public var body:some View {
        LBEmojiBadgeView(emoji: "📷", rimColor: Color("RimColorInstagram", bundle: .module))
    }
    public init() {
        
    }
}
