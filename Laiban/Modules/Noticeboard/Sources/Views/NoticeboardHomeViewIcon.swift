
import SwiftUI

public struct NoticeboardHomeViewIcon : View {
    public var body:some View {
        LBEmojiBadgeView(emoji: "💁", rimColor: Color("RimColorNoticeboard", bundle: .module))
    }
    public init() {
        
    }
}
